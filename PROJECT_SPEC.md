# 围护碳研

面向建筑方案设计人员的围护构造工程计算工作台。以构造层的材料、厚度、面积、设计寿命为输入，完成隐含碳与简化热阻的确定性计算，帮助设计人员比较替代构造并保存可追溯的计算书。所有内置物性为教学示例，实际工程须输入经核实的材料参数。

## 实体与规则

- 材料：名称、类别、密度、导热系数、单位质量碳因子、参考寿命、参数来源。提供只读示例和自定义材料。
- 构造：名称、建筑部位、面积、计算年限、碳强度目标、热传递上限、说明、按室外到室内排列的构造层。
- 构造层：稳定标识、材料标识、厚度、施工损耗、替换寿命；同材料可出现在不同层。
- 围护组合：名称、说明、同一建筑的多个部位引用及每个部位在组合中的实际面积、组合修订号。
- 组合部位：引用已保存构造，加入时冻结构造快照与所用材料参数及内容校验值；组合面积仅属于组合，不写回原构造。
- 组合口径：各部位隐含碳为冻结强度乘以组合面积。计算年限不同的构造不得直接混算，只给分年限小计与各部位结果；年限一致时才给组合总量与按总面积折算的平均强度。
- 引用漂移：原构造内容被修改后，组合明确提示选择「保留冻结版本」或「更新为当前版本」，未确认前结果保持冻结值；原构造被删除时仍可按快照计算。
- 构造状态：编辑中 → 已定稿 → 重新编辑。定稿构造不允许直接修改；重新编辑不影响已有计算书。
- 计算书：冻结材料参数、构造和计算结果，记录修订号与计算方法。构造变化不追溯修改历史计算书。
- 比较必须使用相同建筑部位、面积与年限。差异提示明确，支持将替代构造调整为基准口径。

## 计算方法

每平方米初始质量 = 厚度（毫米）÷ 1000 × 密度；初始隐含碳 = 质量 × 碳因子 ×（1 + 损耗率）。替换次数 = ceil(计算年限 / 材料寿命) − 1，边界年份不计入新一轮替换。生命周期隐含碳为初始与替换之和。此模型只计材料生产和同因子替换，不包含运行能耗、运输、施工能耗与终结阶段。

简化热阻为各层厚度（米）÷ 导热系数加内外表面热阻 0.11 与 0.04；传热系数为总热阻倒数。计算不含热桥、含湿修正与空腔修正。

## 连通流程

1. 构造编辑：新建构造 → 输入基本条件 → 选择材料并添加层 → 调整厚度与损耗 → 检查结果 → 保存 → 刷新恢复。
2. 替代方案：复制现有构造 → 更换材料或厚度 → 保存 → 选择两种构造 → 校验同口径 → 比较差值与层贡献。
3. 计算书定稿：保存有效构造 → 生成冻结计算书 → 下载中文文本 → 重新编辑 → 验证旧计算书保持原值。
4. 围护组合：新建组合 → 从已保存构造选择外墙、屋面、楼板等多个部位 → 分别填写组合内实际面积 → 查看各部分隐含碳、组合总量与折算强度 → 保存冻结 → 重新打开调整；年限不同则分组展示，不混算；引用构造被修改后显式选择保留或更新。

## 模块与组件边界

- workspace：根组件仅负责布置导航和工作面；工作区组合函数编排动作和统一错误反馈。
- assemblies：构造表单接收构造副本并发出更新；层编辑器接收层及材料并发出修改、移除和移动事件；构造列表仅发出选择、创建、复制事件。
- materials：参数目录与自定义材料表单负责选择、物性输入和来源解释。
- carbon：独立纯函数完成物理校验、计算与目标判断，结果组件仅接收计算结果。
- comparison：比较选择器接收构造集合并发出基准与替代标识；比较逻辑拒绝不同口径。
- schemes：组合纯函数基于冻结快照与冻结材料计算，禁止跨年限混算；漂移检测以构造内容校验值为准；组合组件只发出增删部位、面积修改、保留与更新引用事件，不修改原构造。
- documents：定稿动作冻结输入和结果，计算书组件展示历史版本并提供下载。
- persistence：使用项目独立浏览器存储命名空间。保存时在浏览器互斥锁内核对修订标识，跨标签页变化提示重新加载；存储失败不提交内存状态。监听器和下载资源在使用结束后释放。

## 视觉与语言

内部工程工具：顶部紧凑导航、浅暖纸色工作区、深绿主要动作、构造层纵向编辑、内联物理参数和计算结果。首屏直接展示构造工作面，不使用通用管理后台布局。窄屏垂直排列，所有交互支持键盘和清晰标签。

## 验证边界

本次为无测试初始化基线，testing 为 deferred；不提供单元测试、测试夹具或完整浏览器 E2E 测试套件。后续代码测试任务负责添加公式边界、存储故障和跨标签页并发的系统性测试。当前仍通过有界生产冒烟命令在真实浏览器页面执行三条流程；另执行严格类型检查、生产构建、领域与隔离审计。

## 范围

纯前端，无远程接口，浏览器保存仅限当前源。输入物性不代表权威工程凭证。下载计算书用于留存计算过程。无多人协同服务。
